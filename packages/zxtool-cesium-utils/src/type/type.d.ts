type Num2 = [number, number]
type Num3 = [number, number, number]
type LonLat = Num2
type LonLatObj = {
  lon: number
  lat: number
}
type LonLatHeight = Num3
type LonLatHeightObj = {
  lon: number
  lat: number
  height: number
}
